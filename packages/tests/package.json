{
  "name": "@vietdub/tests",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "prebuild": "npm run build -w @vietdub/backend",
    "test": "vitest run",
    "pretest": "npm run build -w @vietdub/backend",
    "test:unit": "vitest run unit",
    "pretest:unit": "npm run build -w @vietdub/backend",
    "test:integration": "vitest run integration",
    "pretest:integration": "npm run build -w @vietdub/backend",
    "test:spike": "node dist/spike/run-spike.js",
    "pretest:e2e": "npm run build -w @vietdub/backend",
    "test:e2e": "playwright test",
    "pretest:e2e:chrome": "npm run build -w @vietdub/backend",
    "test:e2e:chrome": "playwright test --project=chromium",
    "pretest:e2e:firefox": "npm run build -w @vietdub/backend",
    "test:e2e:firefox": "playwright test --project=firefox",
    "benchmark": "tsx src/benchmarks/run-benchmarks.ts",
    "build": "tsc -b"
  },
  "dependencies": {
    "@vietdub/shared": "*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@types/node": "^22.10.2",
    "@types/ws": "^8.5.13",
    "playwright": "^1.50.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
